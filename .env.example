# ============================================================
# SEO CIRO — Variables de entorno
# Copia este archivo como .env y rellena los valores reales
# ============================================================

# Base de datos PostgreSQL
DATABASE_URL="postgresql://USUARIO:PASSWORD@HOST:PUERTO/NOMBRE_DB"

# Usadas por docker-compose.yml para levantar el contenedor de Postgres
# (DATABASE_URL se sobrescribe automáticamente en producción para apuntar a "db")
POSTGRES_USER="seo"
POSTGRES_PASSWORD="CAMBIA_ESTO"
POSTGRES_DB="seo"

# NextAuth — genera un secreto con: openssl rand -base64 32
NEXTAUTH_SECRET="CAMBIA_ESTO_POR_UN_SECRETO_ALEATORIO"
NEXTAUTH_URL="https://tudominio.com"

# Encriptación AES-256 para futuros secretos (tokens OAuth, API keys de terceros)
# Exactamente 32 caracteres — genera con: openssl rand -hex 16
ENCRYPTION_KEY="CADENA_ALEATORIA_DE_EXACTAMENTE_32__"

# OpenRouter — Módulo 3 (Título y Meta), Módulo 4 (Schema), Módulo 1 (estructura
# de URLs) y Módulo 7 (Contenido). Permite usar cualquier modelo (Claude, GPT,
# Gemini...) con una sola API key.
OPENROUTER_API_KEY="CAMBIA_ESTO"
OPENROUTER_MODEL="openai/gpt-4o-mini"

# DataForSEO — Módulo 1 (Keyword Research). Auth HTTP Basic
# (Buffer.from(`${login}:${password}`).toString("base64")). Cuenta ya existente
# de la agencia. Dos endpoints: volumen/competición/CPC (Keywords Data API) e
# intención de búsqueda (DataForSEO Labs Search Intent).
DATAFORSEO_LOGIN="CAMBIA_ESTO"
DATAFORSEO_PASSWORD="CAMBIA_ESTO"

# Google OAuth2 — Módulo 6 (Search Console + GA4). Genera las credenciales en
# Google Cloud Console > APIs y servicios > Credenciales. El redirect URI debe
# coincidir exactamente con el configurado ahí (añade tanto la URL de
# desarrollo como la de producción como "URI de redirección autorizados").
GOOGLE_CLIENT_ID="CAMBIA_ESTO"
GOOGLE_CLIENT_SECRET="CAMBIA_ESTO"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/google/oauth/callback"

# PageSpeed Insights — Módulo 8 (Auditoría Técnica). Sin OAuth, clave de API simple
# de Google Cloud Console. Solo se usa sobre la home del proyecto auditado, no
# sobre cada página rastreada (ver docs/04-modelo-de-datos.md, AuditRun).
PAGESPEED_API_KEY="CAMBIA_ESTO"
